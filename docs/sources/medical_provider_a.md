# Medical Provider A — Data Dictionary
**File:** `medical_provider_a.csv` (CSV)  
**DOB:** `MM/DD/YYYY`  
**Relationship:** `EMP`/`SPS`/`DEP`

Key fields: `subscriber_id`, `dependent_seq`, `member_id`, `first_nm`, `last_nm`, `dob`,
`addr_line1`, `city`, `state`, `zip_cd`, `relationship_cd`, `medical_plan_code`, `plan_tier`, `eligibility_status`.

Internal fields to ignore: `internal_batch_id`, `internal_row_checksum`.
