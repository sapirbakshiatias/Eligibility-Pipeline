# Medical Provider C — Data Dictionary
**File:** `medical_provider_c.jsonl` (JSON Lines)  
**DOB:** split across `dob_year`, `dob_month`, `dob_day`

Key fields: `emp_id`, `person_type`, `dep_num`, `member_identifier`, `name.first`, `name.last`,
`address.*`, `plan.plan_id`, `plan.tier`, `eligibility`.

Internal field: `internal_notes`.
